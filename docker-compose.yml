version: '3'
services:
  springboot-app:
    image: h2db:1
    container_name: springboot-app
    ports:
      - "8080:8080"
    depends_on:
      - mysql-db

  mysql-db:
    image: mysql:latest
    container_name: mysql-db
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD= 
      - MYSQL_DATABASE=
